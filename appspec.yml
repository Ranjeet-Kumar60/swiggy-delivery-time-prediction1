version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/app
    overwrite: yes

hooks:
  ApplicationStart:
    - location: scripts/start_docker.sh
      timeout: 600
      runas: root
